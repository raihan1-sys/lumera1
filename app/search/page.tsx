import {searchProducts} from '@/lib/catalog';import {ProductGrid} from '@/components/product-grid';
export default async function SearchPage({searchParams}:{searchParams:Promise<{q?:string;cat?:string}>}){const {q='',cat}=await searchParams;const products=q?searchProducts(q,48,cat):[];return <div className="search-page"><p>SEARCH LUMERA</p><h1>{q?`Results for “${q}”`:'Find something extraordinary'}</h1><span>{q?`${products.length} curated matches in the live catalog.`:'Use the search bar above to explore thousands of products.'}</span>{q&&<ProductGrid products={products} title="Search results"/>}</div>}
