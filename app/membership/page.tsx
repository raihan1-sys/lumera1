'use client';import {Check,Star} from 'lucide-react';import {useState} from 'react';const plans:[string,string,string,string[]][]=[['ESSENTIAL','Free','$0',['Wishlist and saved products','Order tracking','Member-only collections']],['SELECT','Most Popular','$9/mo',['Everything in Essential','Priority support','Early collection access','Private offers']],['PRIVATE','For collectors','$29/mo',['Everything in Select','Concierge sourcing','Extended return window','Private previews']]];
export default function Membership(){const [selected,setSelected]=useState('');return <div className="membership"><section className="membership-hero"><p>LUMERA SELECT</p><h1>More than a membership.</h1><span>Access, service and discovery designed around the way you shop.</span></section><div className="plans">{plans.map(([name,tag,price,features])=><article className={name==='SELECT'?'featured':''} key={name}>{name==='SELECT'&&<b className="popular"><Star/> RECOMMENDED</b>}<p>{tag}</p><h2>{name}</h2><h3>{price}</h3><div>{features.map(f=><span key={f}><Check/> {f}</span>)}</div><button className="gold-button" onClick={()=>setSelected(name)}>CHOOSE {name}</button></article>)}</div>{selected&&<div className="toast">{selected} has been selected for your demo account.</div>}</div>}
