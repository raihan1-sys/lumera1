'use client';import Link from 'next/link';import {Heart} from 'lucide-react';import {ALL_PRODUCTS} from '@/lib/catalog';import {ProductCard} from '@/components/product-card';import {useStore} from '@/components/store-provider';
export default function Wishlist(){const {wish}=useStore();const items=wish.map(id=>ALL_PRODUCTS.find(p=>p.id===id)).filter((p):p is NonNullable<typeof p>=>!!p);return <div className="wishlist-page"><div className="page-hero mini"><p>YOUR EDIT</p><h1>Wishlist</h1><span>Keep the pieces that deserve a second look.</span></div>{items.length?<div className="wishlist-grid">{items.map(p=><ProductCard key={p.id} p={p}/>)}</div>:<div className="empty"><Heart/><h2>Nothing saved yet.</h2><Link className="gold-button" href="/shop">EXPLORE LUMERA</Link></div>}</div>}
