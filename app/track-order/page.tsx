'use client';import {useState} from 'react';import {Search,PackageCheck} from 'lucide-react';export default function Track(){const [found,setFound]=useState(false);return <div className="track"><section><p>ORDER STATUS</p><h1>Where is my order?</h1><span>Enter your order number and email to check your delivery progress.</span><form onSubmit={e=>{e.preventDefault();setFound(true)}}><input placeholder="Order number (e.g. LM-2026-1024)" required/><input type="email" placeholder="Email address" required/><button className="gold-button"><Search/> TRACK ORDER</button></form></section>{found&&<aside><PackageCheck/><p>DEMO ORDER FOUND</p><h2>Your collection is being prepared.</h2><div className="timeline"><span className="done">Order placed</span><span className="done">Payment demo approved</span><span className="current">Preparing shipment</span><span>Out for delivery</span></div></aside>}</div>}
