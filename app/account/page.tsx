'use client';import {useState} from 'react';import {User,Package,MapPin,Heart,LogIn} from 'lucide-react';
export default function Account(){const [tab,setTab]=useState('signin');const [signed,setSigned]=useState(false);if(signed)return <div className="account"><div className="account-hero"><p>MY LUMERA</p><h1>Welcome back.</h1><span>Your private space for orders, saved pieces and addresses.</span></div><div className="account-dashboard"><button><Package/>Orders <small>No completed orders yet</small></button><button><Heart/>Saved pieces <small>View your wishlist</small></button><button><MapPin/>Addresses <small>Manage delivery locations</small></button><button><User/>Profile <small>Update account details</small></button></div></div>;return <div className="auth-wrap"><section className="auth-art"><p>LUMERA ACCOUNT</p><h1>A more personal marketplace.</h1><span>Save your edit, follow your orders and return to the pieces you love.</span></section><section className="auth-card"><div className="auth-tabs"><button className={tab==='signin'?'active':''} onClick={()=>setTab('signin')}>SIGN IN</button><button className={tab==='signup'?'active':''} onClick={()=>setTab('signup')}>CREATE ACCOUNT</button></div><h2>{tab==='signin'?'Welcome back':'Join Lumera'}</h2><form onSubmit={e=>{e.preventDefault();setSigned(true)}}>{tab==='signup'&&<input placeholder="Full name" required/>}<input type="email" placeholder="Email address" required/><input type="password" placeholder="Password" required/><button className="gold-button"><LogIn/> {tab==='signin'?'SIGN IN':'CREATE MY ACCOUNT'}</button></form><small>Demo authentication for this portfolio project.</small></section></div>}
